@model List<Turismo_Bova.Models.Servicio>
@{
    ViewData["Title"] = "Servicios";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1>Servicios</h1>
        <a asp-action="Crear" class="btn btn-success">
            <i class="fas fa-plus"></i> Nuevo Servicio
        </a>
    </div>

    <div class="card border-primary">
        <div class="card-body">
            <table id="tablaServicios" class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Nombre</th>
                        <th>Descripción</th>
                        <th>Precio Base</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var s in Model)
                    {
                        <tr>
                            <td>@s.Nombre</td>
                            <td>@s.Descripcion</td>
                            <td>@s.PrecioBase.ToString("C")</td>
                            <td>
                                <a asp-action="Editar" asp-route-id="@s.Id" class="btn btn-warning btn-sm me-1">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <button type="button" class="btn btn-danger btn-sm btn-eliminar" data-id="@s.Id">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- 🔁 Formulario oculto para eliminar -->
<form id="form-eliminar" method="post" asp-action="EliminarConfirmar" style="display:none;">
    <input type="hidden" name="id" id="input-id" />
</form>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#tablaServicios').DataTable({
                language: {
                    url: 'https://cdn.datatables.net/plug-ins/1.13.6/i18n/es-ES.json'
                }
            });

            // Confirmación SweetAlert para eliminar
            document.querySelectorAll('.btn-eliminar').forEach(button => {
                button.addEventListener('click', function () {
                    const id = this.dataset.id;

                    Swal.fire({
                        title: '¿Eliminar este servicio?',
                        text: "Esta acción no se puede deshacer.",
                        icon: 'warning',
                        showCancelButton: true,
                        confirmButtonColor: '#3085d6',
                        cancelButtonColor: '#d33',
                        confirmButtonText: 'Sí, eliminar',
                        cancelButtonText: 'Cancelar'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            document.getElementById('input-id').value = id;
                            document.getElementById('form-eliminar').submit();
                        }
                    });
                });
            });
        });
    </script>
}
